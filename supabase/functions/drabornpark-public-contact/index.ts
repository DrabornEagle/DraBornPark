import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "jsr:@supabase/supabase-js@2";

const VERSION="0.4.0";
const corsHeaders={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"GET, POST, OPTIONS","Content-Type":"application/json; charset=utf-8","Cache-Control":"no-store"};
type Priority="normal"|"high"|"emergency";
const categories:Record<string,{title:string;body:string;priority:Priority}>={blocked_exit:{title:"Aracınızı hareket ettirmeniz isteniyor",body:"Bir kullanıcı aracınızın başka bir aracın çıkışını engellediğini bildirdi.",priority:"high"},move_vehicle:{title:"Aracınızı hareket ettirebilir misiniz?",body:"Aracınızın kısa süre içinde hareket ettirilmesi isteniyor.",priority:"normal"},lights_on:{title:"Farlarınız açık olabilir",body:"Aracınız için farların açık olabileceğine dair bildirim gönderildi.",priority:"normal"},window_open:{title:"Camınız açık olabilir",body:"Aracınızın camlarından birinin açık olabileceği bildirildi.",priority:"normal"},door_open:{title:"Kapınız açık olabilir",body:"Aracınızın kapılarından birinin açık olabileceği bildirildi.",priority:"high"},trunk_open:{title:"Bagajınız açık olabilir",body:"Aracınızın bagajının açık olabileceği bildirildi.",priority:"high"},damage:{title:"Aracınızda hasar fark edilmiş olabilir",body:"Aracınızla ilgili olası bir hasar bildirimi aldınız.",priority:"high"},suspicious:{title:"Şüpheli durum bildirimi",body:"Aracınızın çevresinde şüpheli bir durum olabileceği bildirildi.",priority:"high"},towing:{title:"Aracınız çekiliyor olabilir",body:"Aracınızın çekilmekte olabileceğine dair yüksek öncelikli bildirim geldi.",priority:"emergency"},animal:{title:"Araçta hayvan var",body:"Araçta bir hayvan bulunduğuna dair acil bildirim gönderildi.",priority:"emergency"},child:{title:"Araçta çocuk var",body:"Araçta bir çocuk bulunduğuna dair acil bildirim gönderildi.",priority:"emergency"},fire:{title:"Duman / yangın şüphesi",body:"Aracınızla ilgili duman veya yangın şüphesi bildirildi.",priority:"emergency"},forgotten_item:{title:"Eşya veya anahtar unutulmuş olabilir",body:"Aracınızda eşya veya anahtar unutulmuş olabileceği bildirildi.",priority:"normal"},witness:{title:"Bir olaya şahit olundu",body:"Aracınızla ilgili bir olaya şahit olan kullanıcı bildirim gönderdi.",priority:"high"},emergency:{title:"Acil durum bildirimi",body:"Aracınızla ilgili acil müdahale gerektirebilecek bir durum bildirildi.",priority:"emergency"},other:{title:"Yeni araç bildirimi",body:"DraBornPark üzerinden aracınızla ilgili yeni bir bildirim gönderildi.",priority:"normal"}};
const json=(data:unknown,status=200)=>new Response(JSON.stringify(data),{status,headers:corsHeaders});
function normalizeTagCode(input:string){const cleaned=input.trim().toUpperCase().replace(/[^A-Z0-9-]/g,"");return cleaned.startsWith("DP-")?cleaned:`DP-${cleaned}`}
async function sha256(value:string){const hash=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(value));return Array.from(new Uint8Array(hash)).map(b=>b.toString(16).padStart(2,"0")).join("")}
function sanitizeMessage(input?:string){if(!input)return null;let v=input.trim().slice(0,700);v=v.replace(/(?:\+?90\s*)?(?:0\s*)?5\d{2}[\s.-]*\d{3}[\s.-]*\d{2}[\s.-]*\d{2}/g,"[telefon numarası gizlendi]");v=v.replace(/[\w.+-]+@[\w.-]+\.[A-Za-z]{2,}/g,"[e-posta gizlendi]");if(/(amk|aq|orospu|siktir|piç|gerizekalı|salak|aptal|lan\b)/gi.test(v))return "Araçla ilgili serbest mesaj DraBornPark güvenlik filtresi tarafından nötrleştirildi. Lütfen bildirimin kategorisini dikkate alın.";return v}
async function hitRateLimit(db:any,key:string,limit=6,windowSeconds=600,blockSeconds=1800){const {data,error}=await db.rpc("drabornpark_hit_rate_limit",{drabornpark_bucket_key:key,drabornpark_limit:limit,drabornpark_window_seconds:windowSeconds,drabornpark_block_seconds:blockSeconds});if(error)throw error;const r=Array.isArray(data)?data[0]:data;return{blocked:Boolean(r?.blocked),retryAfter:Number(r?.retry_after??0)}}
function localClock(timeZone:string){const parts=new Intl.DateTimeFormat("en-GB",{timeZone,weekday:"short",hour:"2-digit",minute:"2-digit",hour12:false}).formatToParts(new Date());const map=Object.fromEntries(parts.map(p=>[p.type,p.value]));const dayMap:Record<string,number>={Mon:1,Tue:2,Wed:3,Thu:4,Fri:5,Sat:6,Sun:7};return{day:dayMap[map.weekday]??1,time:`${map.hour??"00"}:${map.minute??"00"}`}}
function ruleMatches(rule:any,day:number,time:string){const days=Array.isArray(rule.days_of_week)?rule.days_of_week.map(Number):[];const start=String(rule.start_time??"00:00").slice(0,5);const end=String(rule.end_time??"23:59").slice(0,5);if(start<=end)return days.includes(day)&&time>=start&&time<=end;if(time>=start)return days.includes(day);const previous=day===1?7:day-1;return time<=end&&days.includes(previous)}
async function resolveRecipients(db:any,ownerUserId:string,vehicleId:string|null,priority:Priority){const {data:profile}=await db.from("drabornpark_profiles").select("notification_settings").eq("user_id",ownerUserId).maybeSingle();const settings=profile?.notification_settings||{};const tz=String(settings.timezone||"Europe/Istanbul");let clock;try{clock=localClock(tz)}catch{clock=localClock("Europe/Istanbul")}let q=db.from("drabornpark_routing_rules").select("id,vehicle_id,days_of_week,start_time,end_time,target_type,target_user_id,updated_at").eq("owner_user_id",ownerUserId).eq("is_enabled",true);q=vehicleId?q.or(`vehicle_id.is.null,vehicle_id.eq.${vehicleId}`):q.is("vehicle_id",null);const {data:rules}=await q;const sorted=[...(rules??[])].sort((a:any,b:any)=>{const ax=vehicleId&&a.vehicle_id===vehicleId?1:0;const bx=vehicleId&&b.vehicle_id===vehicleId?1:0;if(ax!==bx)return bx-ax;return new Date(b.updated_at??0).getTime()-new Date(a.updated_at??0).getTime()});const active=sorted.find((r:any)=>ruleMatches(r,clock.day,clock.time));let routed=ownerUserId;if(active?.target_type==="family"&&active.target_user_id){const {data:family}=await db.from("drabornpark_family_members").select("id").eq("owner_user_id",ownerUserId).eq("member_user_id",active.target_user_id).eq("status","active").eq("can_receive_notifications",true).maybeSingle();if(family)routed=active.target_user_id}return{recipients:[...new Set(priority==="emergency"?[ownerUserId,routed]:[routed])],routingRuleId:active?.id??null}}
async function sendExpoPush(db:any,userIds:string[],title:string,body:string,reportId:string,priority:Priority){if(!userIds.length)return;const {data:tokens}=await db.from("drabornpark_push_tokens").select("user_id,expo_push_token").in("user_id",userIds).eq("is_enabled",true);if(!tokens?.length)return;const messages=tokens.map((t:any)=>({to:t.expo_push_token,title,body,sound:"default",priority:priority==="emergency"?"high":"default",data:{type:"drabornpark_report",reportId}}));try{await fetch("https://exp.host/--/api/v2/push/send",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(messages)})}catch(e){console.error("push_delivery_error",e)}}

Deno.serve(async(req)=>{if(req.method==="OPTIONS")return new Response("ok",{headers:corsHeaders});if(req.method==="GET")return json({ok:true,service:"drabornpark-public-contact",version:VERSION,publicWeb:"https://draborneagle.com/DraBornPark/"});if(req.method!=="POST")return json({error:"method_not_allowed"},405);const url=Deno.env.get("SUPABASE_URL")??"";const key=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")??"";if(!url||!key)return json({error:"server_configuration_error"},500);const db=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}});try{const payload=await req.json();const action=String(payload?.action??"lookup");const ip=req.headers.get("x-forwarded-for")?.split(",")[0]?.trim()||req.headers.get("cf-connecting-ip")||"unknown";const ipHash=await sha256(ip);const userAgent=(req.headers.get("user-agent")||"unknown").slice(0,400);
if(action==="lookup"){const tagCode=normalizeTagCode(String(payload?.tagCode??""));const rate=await hitRateLimit(db,`${ipHash}:lookup`,30,600,600);if(rate.blocked)return json({error:"rate_limited",retryAfter:rate.retryAfter},429);const {data:snapshot,error}=await db.rpc("drabornpark_public_tag_snapshot",{drabornpark_tag_code:tagCode});if(error)throw error;if(!snapshot)return json({error:"tag_not_found"},404);const {data:tag}=await db.from("drabornpark_tags").select("id").eq("tag_code",tagCode).maybeSingle();if(tag?.id)await db.from("drabornpark_scan_events").insert({tag_id:tag.id,ip_hash:ipHash,user_agent:userAgent,action:"lookup"});return json({ok:true,version:VERSION,snapshot,categories:Object.entries(categories).map(([key,value])=>({key,...value}))})}
if(action==="notify"){const tagCode=normalizeTagCode(String(payload?.tagCode??""));const sessionKey=String(payload?.sessionKey??crypto.randomUUID()).slice(0,100);const categoryKey=String(payload?.category??"other");const category=categories[categoryKey]??categories.other;const rate=await hitRateLimit(db,`${ipHash}:${tagCode}`);if(rate.blocked)return json({error:"rate_limited",retryAfter:rate.retryAfter},429);const {data:tag}=await db.from("drabornpark_tags").select("id,vehicle_id,owner_user_id,status").eq("tag_code",tagCode).maybeSingle();if(!tag||tag.status!=="ACTIVATED"||!tag.owner_user_id)return json({error:"tag_not_available"},404);const original=payload?.message?String(payload.message).slice(0,700):null;const safe=sanitizeMessage(original)||category.body;const {data:report,error:reportError}=await db.from("drabornpark_reports").insert({tag_id:tag.id,vehicle_id:tag.vehicle_id,owner_user_id:tag.owner_user_id,category:categoryKey,priority:category.priority,message_original:original,message_safe:safe,sender_session_key:sessionKey,sender_ip_hash:ipHash,sender_user_agent:userAgent}).select("id").single();if(reportError)throw reportError;const {data:session,error:sessionError}=await db.from("drabornpark_contact_sessions").insert({report_id:report.id,tag_id:tag.id,owner_user_id:tag.owner_user_id}).select("public_token,expires_at").single();if(sessionError)throw sessionError;const routing=await resolveRecipients(db,tag.owner_user_id,tag.vehicle_id,category.priority);await db.from("drabornpark_scan_events").insert({tag_id:tag.id,session_key:sessionKey,ip_hash:ipHash,user_agent:userAgent,action:"notify"});await db.from("drabornpark_timeline_events").insert({owner_user_id:tag.owner_user_id,vehicle_id:tag.vehicle_id,event_type:"REPORT_RECEIVED",title:category.title,description:safe,metadata:{reportId:report.id,priority:category.priority,routingRuleId:routing.routingRuleId,routedRecipients:routing.recipients.length}});await sendExpoPush(db,routing.recipients,category.title,safe,report.id,category.priority);return json({ok:true,reportId:report.id,sessionToken:session.public_token,expiresAt:session.expires_at,ownerMessage:"Bildirim DraBornPark üzerinden güvenli şekilde iletildi."},201)}
if(action==="chat"){const token=String(payload?.sessionToken??"");const bodyOriginal=String(payload?.message??"").trim().slice(0,700);if(!token||!bodyOriginal)return json({error:"invalid_chat_payload"},400);const {data:session}=await db.from("drabornpark_contact_sessions").select("id,status,expires_at,tag_id").eq("public_token",token).maybeSingle();if(!session||session.status!=="open"||new Date(session.expires_at)<=new Date())return json({error:"session_closed"},410);const rate=await hitRateLimit(db,`${ipHash}:chat:${token}`,12,600,1200);if(rate.blocked)return json({error:"rate_limited",retryAfter:rate.retryAfter},429);const bodySafe=sanitizeMessage(bodyOriginal)||"Mesaj güvenlik filtresi tarafından temizlendi.";await db.from("drabornpark_messages").insert({session_id:session.id,sender_role:"visitor",body_original:bodyOriginal,body_safe:bodySafe});await db.from("drabornpark_contact_sessions").update({last_activity_at:new Date().toISOString()}).eq("id",session.id);await db.from("drabornpark_scan_events").insert({tag_id:session.tag_id,ip_hash:ipHash,user_agent:userAgent,action:"chat"});return json({ok:true,message:bodySafe},201)}
if(action==="status"){const token=String(payload?.sessionToken??"");const {data:session}=await db.from("drabornpark_contact_sessions").select("id,status,expires_at").eq("public_token",token).maybeSingle();if(!session)return json({error:"session_not_found"},404);if(session.status!=="open"||new Date(session.expires_at)<=new Date())return json({error:"session_closed"},410);const {data:messages}=await db.from("drabornpark_messages").select("id,sender_role,body_safe,created_at").eq("session_id",session.id).order("created_at",{ascending:true}).limit(50);return json({ok:true,status:session.status,expiresAt:session.expires_at,messages:messages??[]})}
return json({error:"unknown_action"},400)}catch(e){console.error(e);return json({error:"request_failed"},500)}});
