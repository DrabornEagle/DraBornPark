# DraBornPark Developer APK build trigger

This short-lived branch exists only to run the full v0.5.0 / Android versionCode 13 CI verification and Developer APK artifact build against the finalized main source.
