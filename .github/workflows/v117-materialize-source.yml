name: DraBornPark v1.0.17 Materialized Source

on:
  push:
    branches: [main]
    paths:
      - '.github/V117_MATERIALIZE_TRIGGER'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  materialize-v117:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Materialize and validate v1.0.17
        run: |
          set -euo pipefail
          npm ci --no-audit --no-fund
          npm run check
          npm run typecheck
          sed -i 's/const VERSION="1.0.16";/const VERSION="1.0.17";/' supabase/functions/dkd-drabornpark-google-play/index.ts
      - name: Commit materialized v1.0.17 source
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'DrabornEagle Release Bot'
          git config user.email 'draborneagle@gmail.com'
          git add app/index.tsx app/hub.tsx app/legal.tsx app/factory.tsx app/admin.tsx src/components/MandatoryUpdateGate.tsx package-lock.json supabase/functions/dkd-drabornpark-google-play/index.ts supabase/functions/dkd-drabornpark-app-version/index.ts
          if git diff --cached --quiet; then
            echo 'Materialized source already committed.'
            exit 0
          fi
          git commit -m 'feat: materialize DraBornPark v1.0.17 source'
          git push origin HEAD:main
