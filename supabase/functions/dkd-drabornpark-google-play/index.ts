import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import {createClient} from "jsr:@supabase/supabase-js@2";

const VERSION="1.0.23";
const PACKAGE_NAME=Deno.env.get("GOOGLE_PLAY_PACKAGE_NAME")||"com.draborneagle.drabornpark";
const PRODUCT_ID="drabornpark_plus";
const MONTHLY_PLAN=Deno.env.get("GOOGLE_PLAY_MONTHLY_BASE_PLAN_ID")||"monthly";
const YEARLY_PLAN=Deno.env.get("GOOGLE_PLAY_YEARLY_BASE_PLAN_ID")||"yearly";
const corsHeaders={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST, OPTIONS","Content-Type":"application/json; charset=utf-8","Cache-Control":"no-store"};
const json=(data:unknown,status=200)=>new Response(JSON.stringify(data),{status,headers:corsHeaders});
const encoder=new TextEncoder();
function b64url(input:Uint8Array|string){const bytes=typeof input==="string"?encoder.encode(input):input;let binary="";for(const dkd_byte of bytes)binary+=String.fromCharCode(dkd_byte);return btoa(binary).replace(/=/g,"").replace(/\+/g,"-").replace(/\//g,"_");}
function pemBytes(pem:string){const body=pem.replace(/-----BEGIN PRIVATE KEY-----/g,"").replace(/-----END PRIVATE KEY-----/g,"").replace(/\s+/g,"");const binary=atob(body);const bytes=new Uint8Array(binary.length);for(let dkd_index=0;dkd_index<binary.length;dkd_index++)bytes[dkd_index]=binary.charCodeAt(dkd_index);return bytes;}
async function sha256(value:string){const hash=await crypto.subtle.digest("SHA-256",encoder.encode(value));return Array.from(new Uint8Array(hash)).map(dkd_byte=>dkd_byte.toString(16).padStart(2,"0")).join("");}
async function googleServiceAccount(adminDb:any){let raw=Deno.env.get("GOOGLE_PLAY_SERVICE_ACCOUNT_JSON")||"";if(!raw){const {data,error}=await adminDb.rpc("dkd_drabornpark_get_google_play_service_account");if(error)throw new Error("google_play_vault_read_failed");raw=String(data||"");}if(!raw)throw new Error("google_play_not_configured");let service:any;try{service=JSON.parse(raw);}catch{throw new Error("google_play_credentials_invalid");}if(service?.type!=="service_account"||!service?.client_email||!service?.private_key)throw new Error("google_play_credentials_invalid");return service;}
async function googleAccessToken(adminDb:any){const service=await googleServiceAccount(adminDb);const tokenUri=service.token_uri||"https://oauth2.googleapis.com/token";const now=Math.floor(Date.now()/1000);const header=b64url(JSON.stringify({alg:"RS256",typ:"JWT"}));const claims=b64url(JSON.stringify({iss:service.client_email,scope:"https://www.googleapis.com/auth/androidpublisher",aud:tokenUri,iat:now,exp:now+3600}));const signingInput=`${header}.${claims}`;const key=await crypto.subtle.importKey("pkcs8",pemBytes(service.private_key),{name:"RSASSA-PKCS1-v1_5",hash:"SHA-256"},false,["sign"]);const signature=new Uint8Array(await crypto.subtle.sign("RSASSA-PKCS1-v1_5",key,encoder.encode(signingInput)));const assertion=`${signingInput}.${b64url(signature)}`;const response=await fetch(tokenUri,{method:"POST",headers:{"Content-Type":"application/x-www-form-urlencoded"},body:new URLSearchParams({grant_type:"urn:ietf:params:oauth:grant-type:jwt-bearer",assertion})});const payload=await response.json().catch(()=>({}));if(!response.ok||!payload.access_token){console.error("[DraBornPark Google OAuth]",response.status,payload?.error||"unknown");throw new Error("google_oauth_failed");}return String(payload.access_token);}
async function googleGet(accessToken:string,path:string){const response=await fetch(`https://androidpublisher.googleapis.com/androidpublisher/v3/${path}`,{headers:{Authorization:`Bearer ${accessToken}`,Accept:"application/json"}});const payload=await response.json().catch(()=>({}));return{response,payload};}
async function getGoogleSubscription(accessToken:string,purchaseToken:string){return googleGet(accessToken,`applications/${encodeURIComponent(PACKAGE_NAME)}/purchases/subscriptionsv2/tokens/${encodeURIComponent(purchaseToken)}`);}
async function getGoogleOrder(accessToken:string,orderId:string){return googleGet(accessToken,`applications/${encodeURIComponent(PACKAGE_NAME)}/orders/${encodeURIComponent(orderId)}`);}
async function findFullRefund(accessToken:string,lines:any[],google:any){const dkd_ids=[...new Set(lines.map((dkd_item:any)=>String(dkd_item?.latestSuccessfulOrderId||"")).filter(Boolean))] as string[];if(!dkd_ids.length&&google?.latestOrderId)dkd_ids.push(String(google.latestOrderId));for(const dkd_order_id of dkd_ids){const dkd_order=await getGoogleOrder(accessToken,dkd_order_id);if(!dkd_order.response.ok){console.warn("[DraBornPark Play order]",dkd_order_id,dkd_order.response.status);continue;}const dkd_refund=dkd_order.payload?.orderHistory?.refundEvent;if(dkd_refund)return{refunded:true,orderId:dkd_order_id,event:dkd_refund};}return{refunded:false,orderId:null,event:null};}
function mapState(state:string,expiry:string|null){const expiryMs=expiry?Date.parse(expiry):0;const future=Number.isFinite(expiryMs)&&expiryMs>Date.now();switch(state){case"SUBSCRIPTION_STATE_ACTIVE":return{status:future?"PLUS_ACTIVE":"PLUS_EXPIRED",entitled:future};case"SUBSCRIPTION_STATE_IN_GRACE_PERIOD":return{status:future?"PLUS_GRACE_PERIOD":"PLUS_EXPIRED",entitled:future};case"SUBSCRIPTION_STATE_CANCELED":return{status:future?"PLUS_CANCELLED":"PLUS_EXPIRED",entitled:future};default:return{status:"PLUS_EXPIRED",entitled:false};}}
function cancellationReason(google:any){const context=google?.canceledStateContext||{};if(context?.userInitiatedCancellation)return"USER";if(context?.systemInitiatedCancellation)return"SYSTEM";if(context?.developerInitiatedCancellation)return"DEVELOPER";if(context?.replacementCancellation)return"REPLACEMENT";return"NONE";}
async function acknowledgePending(accessToken:string,purchaseToken:string,google:any){if(String(google?.acknowledgementState||"")!=="ACKNOWLEDGEMENT_STATE_PENDING")return true;const endpoint=`https://androidpublisher.googleapis.com/androidpublisher/v3/applications/${encodeURIComponent(PACKAGE_NAME)}/purchases/subscriptions/${encodeURIComponent(PRODUCT_ID)}/tokens/${encodeURIComponent(purchaseToken)}:acknowledge`;const response=await fetch(endpoint,{method:"POST",headers:{Authorization:`Bearer ${accessToken}`,Accept:"application/json","Content-Type":"application/json"},body:"{}"});if(response.ok)return true;const refreshed=await getGoogleSubscription(accessToken,purchaseToken);return refreshed.response.ok&&String(refreshed.payload?.acknowledgementState||"")==="ACKNOWLEDGEMENT_STATE_ACKNOWLEDGED";}
Deno.serve(async(req:Request)=>{if(req.method==="OPTIONS")return new Response("ok",{headers:corsHeaders});if(req.method!=="POST")return json({error:"method_not_allowed"},405);const url=Deno.env.get("SUPABASE_URL")??"";const anon=Deno.env.get("SUPABASE_ANON_KEY")??"";const serviceKey=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")??"";if(!url||!anon||!serviceKey)return json({error:"server_configuration_error"},500);const authorization=req.headers.get("Authorization")??"";const userDb=createClient(url,anon,{global:{headers:{Authorization:authorization}},auth:{persistSession:false,autoRefreshToken:false}});const adminDb=createClient(url,serviceKey,{auth:{persistSession:false,autoRefreshToken:false}});try{const {data:{user},error:userError}=await userDb.auth.getUser();if(userError||!user)return json({error:"authentication_required"},401);const body=await req.json().catch(()=>({}));const action=String(body?.action??"verify");if(action!=="verify"&&action!=="restore")return json({error:"unknown_action"},400);const purchaseToken=String(body?.purchaseToken??"").trim();const requestedProduct=String(body?.productId??PRODUCT_ID).trim();if(!purchaseToken)return json({error:"purchase_token_required"},400);if(requestedProduct!==PRODUCT_ID)return json({error:"invalid_product"},400);const tokenHash=await sha256(purchaseToken);const {data:existing,error:existingError}=await adminDb.from("drabornpark_subscriptions").select("id,user_id,raw_provider_state").eq("purchase_token_hash",tokenHash).maybeSingle();if(existingError)throw existingError;if(existing&&existing.user_id!==user.id)return json({error:"purchase_already_claimed",accountBound:true,requiresDifferentGooglePlayAccount:true},409);const accessToken=await googleAccessToken(adminDb);const lookup=await getGoogleSubscription(accessToken,purchaseToken);const google=lookup.payload;if(!lookup.response.ok){console.error("[DraBornPark Play verify]",lookup.response.status,google?.error?.status||google?.error?.message||"unknown");return json({error:"google_play_verification_failed",status:lookup.response.status},400);}const lines=Array.isArray(google.lineItems)?google.lineItems:[];const line=lines.find((dkd_item:any)=>String(dkd_item?.productId??"")===PRODUCT_ID);if(!line)return json({error:"product_not_found_in_purchase"},400);const basePlanId=String(line?.offerDetails?.basePlanId??"");if(![MONTHLY_PLAN,YEARLY_PLAN].includes(basePlanId))return json({error:"invalid_base_plan",basePlanId},400);const obfuscated=String(google?.externalAccountIdentifiers?.obfuscatedExternalAccountId??"");const expectedObfuscated=user.id.replace(/-/g,"");const externalAccountMatched=Boolean(obfuscated)&&obfuscated===expectedObfuscated;if(!existing&&!externalAccountMatched)return json({error:obfuscated?"account_mismatch":"purchase_missing_account_binding",accountBound:true,requiresFreshPurchase:true},403);const googleExpiry=line?.expiryTime?String(line.expiryTime):null;const googleExpiryMs=googleExpiry?Date.parse(googleExpiry):0;const state=String(google?.subscriptionState??"");const mapped=mapState(state,googleExpiry);const cancelReason=cancellationReason(google);const autoRenewing=Boolean(line?.autoRenewingPlan?.autoRenewEnabled);const dkdRefund=await findFullRefund(accessToken,lines,google);const refunded=Boolean(dkdRefund.refunded);const shouldAcknowledge=!refunded&&mapped.entitled&&String(google?.acknowledgementState||"")==="ACKNOWLEDGEMENT_STATE_PENDING";let acknowledged=String(google?.acknowledgementState||"")==="ACKNOWLEDGEMENT_STATE_ACKNOWLEDGED";if(shouldAcknowledge){acknowledged=await acknowledgePending(accessToken,purchaseToken,google);if(!acknowledged)return json({error:"google_play_acknowledgement_failed"},502);}const {data:profile,error:profileReadError}=await adminDb.from("drabornpark_profiles").select("plus_trial_until").eq("user_id",user.id).maybeSingle();if(profileReadError)throw profileReadError;const nowMs=Date.now();const purchaseStartMs=google?.startTime?Date.parse(String(google.startTime)):nowMs;const existingV122Bonus=Number((existing as any)?.raw_provider_state?.dkd_v122?.trialBonusMs);const trialUntilMs=profile?.plus_trial_until?Date.parse(profile.plus_trial_until):0;const dkdDayMs=24*60*60*1000;const dkdRemainingTrialMs=Math.max(0,trialUntilMs-purchaseStartMs);const calculatedTrialDays=Math.min(14,dkdRemainingTrialMs>0?Math.ceil(dkdRemainingTrialMs/dkdDayMs):0);const calculatedBonus=calculatedTrialDays*dkdDayMs;const dkdHasV122Bonus=Boolean(existing)&&Number.isFinite(existingV122Bonus)&&existingV122Bonus>=0;const trialBonusMs=dkdHasV122Bonus?existingV122Bonus:calculatedBonus;const trialBonusDays=Math.round(trialBonusMs/dkdDayMs);const trialBonusSource=dkdHasV122Bonus?"same_purchase_token":"current_purchase_trial_snapshot";const previousWasGoogleEntitled=Boolean((existing as any)?.raw_provider_state?.dkd_v122?.wasGoogleEntitled||(existing as any)?.raw_provider_state?.dkd_v120?.wasGoogleEntitled||(existing as any)?.raw_provider_state?.dkd_v119?.wasGoogleEntitled||(existing as any)?.raw_provider_state?.dkd_v115?.wasGoogleEntitled);const wasGoogleEntitled=previousWasGoogleEntitled||mapped.entitled;const effectiveExpiryMs=Number.isFinite(googleExpiryMs)&&googleExpiryMs>0?googleExpiryMs+trialBonusMs:0;const safeExpiredBonusTail=!refunded&&state==="SUBSCRIPTION_STATE_EXPIRED"&&previousWasGoogleEntitled&&!['SYSTEM','DEVELOPER','REPLACEMENT'].includes(cancelReason)&&trialBonusMs>0&&effectiveExpiryMs>nowMs;const effectiveEntitled=!refunded&&(mapped.entitled||safeExpiredBonusTail);const effectiveStatus=refunded?"PLUS_EXPIRED":(mapped.entitled?mapped.status:(safeExpiredBonusTail?"PLUS_CANCELLED":"PLUS_EXPIRED"));const effectiveExpiry=refunded?new Date(nowMs).toISOString():(effectiveExpiryMs>0?new Date(effectiveExpiryMs).toISOString():googleExpiry);const row={user_id:user.id,provider:"google_play",product_id:PRODUCT_ID,base_plan_id:basePlanId,purchase_token_hash:tokenHash,status:effectiveStatus,expires_at:effectiveExpiry,auto_renewing:refunded?false:autoRenewing,last_verified_at:new Date().toISOString(),raw_provider_state:{subscriptionState:state,basePlanId,productId:PRODUCT_ID,acknowledgementState:String(google?.acknowledgementState||""),acknowledged,cancelReason,externalAccountMatched,refunded,refundOrderId:dkdRefund.orderId,refundEvent:dkdRefund.event,dkd_v122:{googleExpiry,purchaseStartTime:google?.startTime??null,trialBonusMs,trialBonusDays,trialBonusSource,effectiveExpiry,wasGoogleEntitled,refunded,refundOrderId:dkdRefund.orderId},dkd_v120:{googleExpiry,purchaseStartTime:google?.startTime??null,trialBonusMs,effectiveExpiry,wasGoogleEntitled,refunded,refundOrderId:dkdRefund.orderId}},updated_at:new Date().toISOString()};if(existing){const {error}=await adminDb.from("drabornpark_subscriptions").update(row).eq("id",existing.id);if(error)throw error;}else{const {error}=await adminDb.from("drabornpark_subscriptions").insert(row);if(error)throw error;}const activeTrial=trialUntilMs>nowMs;const profileStatus=effectiveEntitled?effectiveStatus:(activeTrial?"PLUS_TRIAL":"BASIC");const {error:profileError}=await adminDb.from("drabornpark_profiles").update({subscription_status:profileStatus,updated_at:new Date().toISOString()}).eq("user_id",user.id);if(profileError)throw profileError;return json({ok:true,version:VERSION,entitled:effectiveEntitled,status:effectiveStatus,profileStatus,productId:PRODUCT_ID,basePlanId,expiresAt:effectiveExpiry,googleExpiresAt:googleExpiry,trialBonusMs,trialBonusDays,trialBonusSource,autoRenewing:refunded?false:autoRenewing,acknowledged,cancelReason,externalAccountMatched,subscriptionState:state,refunded,refundOrderId:dkdRefund.orderId,refundEvent:dkdRefund.event});}catch(error){const message=String((error as any)?.message||error||"");console.error(error);if(message==="google_play_not_configured")return json({error:message,setupRequired:true},503);if(message==="google_play_vault_read_failed")return json({error:message},500);if(message==="google_play_credentials_invalid"||message==="google_oauth_failed")return json({error:message},502);return json({error:"verification_failed"},500);}});
