import fs from 'node:fs';
import path from 'node:path';
const root=process.cwd();const full=file=>path.join(root,file);const read=file=>fs.readFileSync(full(file),'utf8');const write=(file,content)=>{fs.mkdirSync(path.dirname(full(file)),{recursive:true});fs.writeFileSync(full(file),content)};
function replaceOnce(file,oldText,newText){const src=read(file);const count=src.split(oldText).length-1;if(count!==1)throw new Error(`v0.5.4 guard ${file}: expected 1 exact match, found ${count}`);write(file,src.replace(oldText,newText));}
function replaceRegex(file,regex,replacement){const src=read(file);const matches=src.match(regex);if(!matches)throw new Error(`v0.5.4 regex guard ${file}: no match for ${regex}`);const again=src.slice((matches.index??0)+matches[0].length).match(regex);if(again)throw new Error(`v0.5.4 regex guard ${file}: more than one match for ${regex}`);write(file,src.replace(regex,()=>replacement));}
function removePath(file){if(fs.existsSync(full(file)))fs.rmSync(full(file),{recursive:true,force:true});}

replaceOnce('src/components/AppChrome.tsx',"label:'Ana'","label:'Ana Sayfa'");
replaceOnce('src/components/AppChrome.tsx',"label:'Park'","label:'Park Alanı'");
replaceOnce('src/components/AppChrome.tsx',"label:'Bildirim'","label:'Bildirimler'");
replaceOnce('app/account.tsx',"import { ACCOUNT_DELETION_URL, DATA_SAFETY_URL, PRIVACY_URL, SUPPORT_URL, TERMS_URL, deleteDraBornParkAccount } from '@/src/lib/account';","import { ACCOUNT_DELETION_URL, DATA_SAFETY_URL, PRIVACY_URL, TERMS_URL, deleteDraBornParkAccount } from '@/src/lib/account';");
replaceOnce('app/account.tsx','<Row icon="lifebuoy" color={palette.blue} title="DraBornPark Destek" body="Destek, veri ve gizlilik talepleri" onPress={()=>Linking.openURL(SUPPORT_URL)}/>','<Row icon="lifebuoy" color={palette.blue} title="DraBornPark Destek" body="Merkezim içindeki Destek Merkezi’ne git" onPress={()=>router.push(\'/feature/support\')}/>');
replaceOnce('app/hub.tsx','label="v0.5.0"','label="v0.5.4"');

replaceOnce('app/index.tsx','label="v0.5.0"','label="v0.5.4"');
replaceOnce('app/index.tsx','<View style={s.featurePair}><FeatureCard icon="chart-donut" color={palette.green} title="İstatistiklerim" body={`${insights.parks} park • ${insights.reports} bildirim`} onPress={()=>router.push(\'/insights\')}/><FeatureCard icon="view-grid-outline" color={palette.purple} title="Merkezim" body="Tüm özelliklere tek yerden ulaş" onPress={()=>router.push(\'/hub\')}/></View>','<View style={s.featurePair}><PlusFeatureCard plus={plus}/><FeatureCard icon="view-grid-outline" color={palette.purple} title="Merkezim" body="Tüm özelliklere tek yerden ulaş" onPress={()=>router.push(\'/hub\')}/></View>');
replaceOnce('app/index.tsx','<Text style={s.liveText}>CANLI ARAÇ</Text>','<Text style={s.liveText}>AKTİF ARAÇ • CANLI</Text>');
replaceOnce('app/index.tsx','s.vehicleCore,{borderColor:`${accent}92`,transform:','s.vehicleCore,{borderColor:`${accent}92`,backgroundColor:`${accent}13`,transform:');
const homeHelpers=`function PlusFeatureCard({plus}:{plus:boolean}){const pulse=useRef(new Animated.Value(0)).current;const sweep=useRef(new Animated.Value(0)).current;useEffect(()=>{const a=Animated.loop(Animated.sequence([Animated.timing(pulse,{toValue:1,duration:920,easing:Easing.inOut(Easing.cubic),useNativeDriver:true}),Animated.timing(pulse,{toValue:0,duration:920,easing:Easing.inOut(Easing.cubic),useNativeDriver:true}),Animated.delay(520)]));const b=Animated.loop(Animated.timing(sweep,{toValue:1,duration:2800,easing:Easing.linear,useNativeDriver:true}));a.start();b.start();return()=>{a.stop();b.stop()};},[pulse,sweep]);return <Animated.View style={{flex:1,transform:[{scale:pulse.interpolate({inputRange:[0,1],outputRange:[1,1.012]})}]}}><Pressable onPress={()=>router.push('/feature/plus')} style={({pressed})=>[s.featureCard,{flex:1,borderColor:palette.yellow+'86',backgroundColor:palette.yellow+'14',overflow:'hidden'},pressed&&s.pressed]}><Animated.View pointerEvents="none" style={{position:'absolute',top:-24,bottom:-24,width:62,backgroundColor:'#FFFFFF12',transform:[{translateX:sweep.interpolate({inputRange:[0,1],outputRange:[-90,360]})},{rotate:'14deg'}]}}/><View style={[s.featureIcon,{backgroundColor:palette.yellow+'28',borderWidth:1,borderColor:palette.orange+'72'}]}><SafeIcon name="crown-outline" size={30} color={palette.yellow}/></View><Text style={s.featureTitle}>DraBornPark+</Text><Text style={s.featureBody}>{plus?'Premium özelliklerin aktif':'14 gün hediye + Google Play planları'}</Text><Animated.View style={{transform:[{translateX:pulse.interpolate({inputRange:[0,1],outputRange:[0,5]})}]}}><SafeIcon name="arrow-top-right" size={19} color={palette.yellow}/></Animated.View><View style={[s.cardLine,{backgroundColor:palette.yellow}]}/></Pressable></Animated.View>}
function ParkPulse({park}:{park:any}){const slot=[park.floor_code?'Kat '+park.floor_code:null,park.zone_name||park.zone_color||null,park.row_code?'Sıra '+park.row_code:null,park.bay_code?'Park '+park.bay_code:null].filter(Boolean).join(' • ');const valid=Number.isFinite(Number(park.latitude))&&Number.isFinite(Number(park.longitude));const coordinates=valid?Number(park.latitude).toFixed(5)+', '+Number(park.longitude).toFixed(5):'';const actual=[park.place_name,slot].filter(Boolean).join(' • ')||coordinates||'Konum ayrıntısı kaydedilmedi';const accuracy=park.accuracy_meters?' • ±'+Math.round(Number(park.accuracy_meters))+' m':'';const detail=coordinates&&park.place_name?coordinates+accuracy:(slot||'Park hafızasındaki son kayıt');return <View style={s.parkPulse}><View style={s.parkPulseIcon}><SafeIcon name="map-marker-check" size={28} color={palette.orange}/></View><View style={{flex:1,minWidth:0}}><Text style={s.parkLabel}>SON PARK</Text><Text numberOfLines={2} style={s.parkTitle}>{actual}</Text><Text numberOfLines={1} style={s.parkBody}>{detail}</Text></View><SafeIcon name="chevron-right" size={24} color={palette.orange}/><View style={[s.cardLine,{backgroundColor:palette.orange}]}/></View>}

const s=StyleSheet.create`;
replaceRegex('app/index.tsx',/function ParkPulse\(\{park\}:\{park:any\}\)\{[\s\S]*?\n\nconst s=StyleSheet\.create/,homeHelpers);

replaceOnce('app/tags.tsx',"const [data,setData]=useState<any>(null);const [tagCode,setTagCode]=useState('');const [transferCode,setTransferCode]=useState('');const [vehicleId,setVehicleId]=useState('');const [busy,setBusy]=useState(false);","const [data,setData]=useState<any>(null);const [tagCode,setTagCode]=useState('');const [transferCode,setTransferCode]=useState('');const [vehicleId,setVehicleId]=useState('');const [busy,setBusy]=useState(false);const [claimOpen,setClaimOpen]=useState(false);");
const claim=`    <Pressable onPress={()=>setClaimOpen(value=>!value)} style={[s.claim,{padding:0,overflow:'hidden'}]}><View style={[s.claimInfo,{padding:17}]}><View style={{width:48,height:48,borderRadius:16,backgroundColor:palette.purple+'20',alignItems:'center',justifyContent:'center'}}><MaterialCommunityIcons name="shield-key-outline" size={29} color={palette.purple}/></View><View style={{flex:1}}><Text style={{color:palette.text,fontSize:type.cardTitle,fontWeight:'900'}}>Etiket devral</Text><Text style={[s.claimBody,{marginTop:4}]}>Başka bir kullanıcıdan gelen etiketi güvenli kodla al</Text></View><MaterialCommunityIcons name={claimOpen?'chevron-up':'chevron-down'} size={28} color={palette.purple}/></View></Pressable>
    {claimOpen?<View style={[s.claim,{marginTop:10}]}><View style={s.claimInfo}><MaterialCommunityIcons name="information-outline" size={25} color={palette.purple}/><Text style={s.claimBody}>Eski araç sahibi 24 saat geçerli kod üretir. Tag ID ve devir kodu eşleşmeden sahiplik değişmez.</Text></View><Field label="TAG ID" value={tagCode} set={setTagCode} placeholder="DP-K7M4X2P9"/><Field label="DEVİR KODU" value={transferCode} set={setTransferCode} placeholder="A1B2C3D4E5F6"/><Text style={s.label}>YENİ ARAÇ</Text><View style={s.vehicles}>{data.vehicles.map((vehicle:any)=><Pressable key={vehicle.id} onPress={()=>setVehicleId(vehicle.id)} style={[s.vehicle,vehicleId===vehicle.id&&s.vehicleOn]}><MaterialCommunityIcons name={vehicle.vehicle_type==='motorcycle'?'motorbike':'car-sports'} size={21} color={vehicleId===vehicle.id?palette.cyan:palette.muted}/><Text style={[s.vehicleText,vehicleId===vehicle.id&&{color:palette.text}]}>{vehicle.vehicle_name}</Text></Pressable>)}</View><Pressable disabled={busy||!tagCode||!transferCode||!vehicleId} onPress={()=>run(()=>claimTagTransfer(tagCode,transferCode,vehicleId),'Etiket hesabına ve seçtiğin araca devredildi.')} style={[s.claimBtn,(busy||!tagCode||!transferCode||!vehicleId)&&{opacity:.45}]}>{busy?<ActivityIndicator color={palette.ink}/>:<><MaterialCommunityIcons name="shield-check-outline" size={23} color={palette.ink}/><Text style={s.claimBtnText}>ETİKETİ GÜVENLİ DEVRAL</Text></>}</Pressable></View>:null}
`;
replaceRegex('app/tags.tsx',/    <SectionHeading title="Etiket devral"[\s\S]*?(?=    <View style=\{s\.info\}>)/,claim);

replaceOnce('app/auth.tsx',"const [password,setPassword]=useState('');","const [password,setPassword]=useState('');\n  const [phone,setPhone]=useState('');");
replaceOnce('app/auth.tsx',"        if(!(await isUsernameAvailable(normalized))){Alert.alert('Kullanıcı adı kullanımda','Başka bir kullanıcı adı seç.');return;}","        if(!(await isUsernameAvailable(normalized))){Alert.alert('Kullanıcı adı kullanımda','Başka bir kullanıcı adı seç.');return;}\n        const digits=phone.replace(/\\D/g,'');\n        const normalizedPhone=phone.trim().startsWith('+')?'+'+digits:digits.startsWith('90')?'+'+digits:digits.startsWith('0')&&digits.length===11?'+90'+digits.slice(1):digits.length===10?'+90'+digits:'+'+digits;\n        if(!/^\\+[1-9]\\d{7,14}$/.test(normalizedPhone)){Alert.alert('Telefon numarasını kontrol et','Telefon numaranı ülke koduyla gir. Türkiye için 05xx... veya +905xx... yazabilirsin.');return;}");
replaceOnce('app/auth.tsx',"const {data,error}=await supabase.auth.signUp({email:email.trim().toLowerCase(),password,options:{data:{display_name:normalized,username:normalized}}});","const {data,error}=await supabase.auth.signUp({email:email.trim().toLowerCase(),password,options:{data:{display_name:normalized,username:normalized,phone_e164:normalizedPhone}}});");
replaceOnce('app/auth.tsx','await bootstrapProfile(normalized,normalized);','await bootstrapProfile(normalized,normalized,undefined,normalizedPhone);');
replaceOnce('app/auth.tsx','        <Field label="KULLANICI ADI *" value={username} set={setUsername} placeholder="draborneagle" icon="account-outline"/>','        <Field label="KULLANICI ADI *" value={username} set={setUsername} placeholder="draborneagle" icon="account-outline"/>\n        <Field label="TELEFON NUMARASI *" value={phone} set={setPhone} placeholder="0555 111 22 33" icon="phone-outline" keyboard="phone-pad"/>');
replaceOnce('app/auth.tsx','<Text style={s.trialTitle}>14 Gün DraBornPark+ Hediye</Text><Text style={s.trialBody}>Yeni kullanıcı deneme süresinde Aile, Vale/Servis, Zaman Kuralları ve gelişmiş araç özelliklerini deneyebilir. Basic etiket işlevleri deneme bitince kapanmaz.</Text>','<Text style={s.trialTitle}>Yeni Etiketine 14 Gün DraBornPark+</Text><Text style={s.trialBody}>Hesabın Basic olarak açılır. Yeni DraBornPark etiketini aktive ettiğinde 14 günlük Premium ödül başlar; süre sonunda aylık veya yıllık Google Play planıyla uygun fiyatla devam edebilirsin.</Text>');

const entitlement=`export function hasPlusEntitlement(profile: any | null, subscription: any | null) {
  if (!profile) return false;
  if (profile.plus_trial_until && new Date(profile.plus_trial_until).getTime() > Date.now()) return true;
  const profileStatus=String(profile.subscription_status||'').toUpperCase();
  const subscriptionStatus=String(subscription?.status||'').toUpperCase();
  if (['PLUS_ACTIVE','PLUS_GRACE_PERIOD','PLUS_CANCELLED'].includes(subscriptionStatus)) return !subscription?.expires_at || new Date(subscription.expires_at).getTime()>Date.now();
  return ['PLUS_ACTIVE','PLUS_GRACE_PERIOD'].includes(profileStatus);
}

export async function isUsernameAvailable`;
replaceRegex('src/lib/drabornpark.ts',/export function hasPlusEntitlement\([\s\S]*?\n\}\n\nexport async function isUsernameAvailable/,entitlement);
const bootstrap=`export async function bootstrapProfile(displayName?: string, username?: string, avatarUrl?: string, phoneE164?: string) {
  const { data: userData } = await supabase.auth.getUser();
  const user = userData.user;
  const meta = user?.user_metadata ?? {};
  const resolvedUsername = (username ?? meta.username ?? displayName ?? meta.display_name ?? user?.email?.split('@')[0] ?? '').trim().toLowerCase() || null;
  const resolvedDisplay = displayName ?? meta.display_name ?? resolvedUsername ?? undefined;
  const { data, error } = await supabase.rpc('dkd_drabornpark_bootstrap_user_v054', {dkd_display_name:resolvedDisplay??null,dkd_username:resolvedUsername,dkd_avatar_url:avatarUrl??meta.avatar_url??null,dkd_phone_e164:phoneE164??meta.phone_e164??null});
  if (error) throw error;
  return data;
}

export async function updateProfile`;
replaceRegex('src/lib/drabornpark.ts',/export async function bootstrapProfile\([\s\S]*?\n\}\n\nexport async function updateProfile/,bootstrap);
const activate=`export async function activateTag(tagCode: string, pin: string, vehicleId: string) {
  const { data, error } = await supabase.rpc('dkd_drabornpark_activate_tag_v054', {dkd_tag_code:tagCode.trim().toUpperCase(),dkd_pin:pin.trim(),dkd_vehicle_id:vehicleId});
  if (error) throw error;
  return data;
}

export async function startTagTransfer`;
replaceRegex('src/lib/drabornpark.ts',/export async function activateTag\([\s\S]*?\n\}\n\nexport async function startTagTransfer/,activate);
const support=`export async function createSupportRequest(subject: string, body: string) {
  const ownerUserId = await currentUserId();
  const { data, error } = await supabase.from('drabornpark_support_requests').insert({owner_user_id:ownerUserId,subject:subject.trim(),body:body.trim(),status:'open'}).select('*').single();
  if (error) throw error;
  void supabase.functions.invoke('dkd-drabornpark-admin-support-push',{body:{supportId:data.id}}).catch(()=>{});
  return data;
}`;
replaceRegex('src/lib/drabornpark.ts',/export async function createSupportRequest\(subject: string, body: string\) \{[\s\S]*?\n\}/,support);

replaceOnce('app/vehicle.tsx',"import { AuroraBackground, ScreenHeader, SectionHeading } from '@/src/components/AppChrome';","import { AuroraBackground, ScreenHeader, SectionHeading } from '@/src/components/AppChrome';\nimport { ColorPopup } from '@/src/components/ColorPopup';");
replaceOnce('app/vehicle.tsx',"const [vehicleType,setVehicleType]=useState<'car'|'motorcycle'>('car');const [name,setName]=useState('');const [plate,setPlate]=useState('');const [brand,setBrand]=useState('');const [model,setModel]=useState('');const [year,setYear]=useState('');const [color,setColor]=useState('');const [busy,setBusy]=useState(false);const [vehicles,setVehicles]=useState<any[]>([]);","const [vehicleType,setVehicleType]=useState<'car'|'motorcycle'>('car');const [name,setName]=useState('');const [plate,setPlate]=useState('');const [brand,setBrand]=useState('');const [model,setModel]=useState('');const [year,setYear]=useState('');const [color,setColor]=useState('');const [busy,setBusy]=useState(false);const [vehicles,setVehicles]=useState<any[]>([]);const [createdVehicle,setCreatedVehicle]=useState<any>(null);");
replaceRegex('app/vehicle.tsx',/  async function save\(\)\{[^\n]+\}\n  return/,`  async function save(){if(!name.trim()){Alert.alert('Araç adı gerekli','Örn. Tiguan, Aile Aracı veya Motorum.');return;}setBusy(true);try{const created=await createVehicle({vehicleName:name,plate,brand,model,modelYear:year?Number(year):null,color,vehicleType});setVehicles(current=>[...current,created]);setName('');setPlate('');setBrand('');setModel('');setYear('');setColor('');Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success);setCreatedVehicle(created);}catch(e:any){Alert.alert('Araç eklenemedi',e?.message||'İşlem başarısız.');}finally{setBusy(false)}}
  return`);
replaceOnce('app/vehicle.tsx','  </ScrollView></SafeAreaView>;','  </ScrollView><ColorPopup visible={Boolean(createdVehicle)} icon="car-check" eyebrow="ARAÇ AĞINA EKLENDİ" title="Aracın hazır!" body={String(createdVehicle?.vehicle_name||\'Aracın\')+\' güvenli DraBornPark ağına eklendi. Şimdi NFC + QR etiketini bağlayabilir veya daha sonra devam edebilirsin.\'} accent={palette.cyan} secondary={palette.pink} primaryLabel="ETİKETİ AKTİVE ET" onPrimary={()=>{setCreatedVehicle(null);router.push(\'/activate/new\')}} secondaryLabel="ARAÇLARIMDA KAL" onSecondary={()=>setCreatedVehicle(null)} chips={[\'ARAÇ PROFİLİ\',\'GÜVENLİ AĞ\',\'NFC + QR HAZIR\']}/></SafeAreaView>;');

replaceOnce('app/activate/[id].tsx',"import { AuroraBackground, ScreenHeader, SectionHeading } from '@/src/components/AppChrome';","import { AuroraBackground, ScreenHeader, SectionHeading } from '@/src/components/AppChrome';\nimport { ColorPopup } from '@/src/components/ColorPopup';");
replaceOnce('app/activate/[id].tsx',"const [vehicleId,setVehicleId]=useState('');const [busy,setBusy]=useState(false);const [loading,setLoading]=useState(true);","const [vehicleId,setVehicleId]=useState('');const [busy,setBusy]=useState(false);const [loading,setLoading]=useState(true);const [reward,setReward]=useState<any>(null);");
replaceRegex('app/activate/[id].tsx',/  async function activate\(\)\{[^\n]+\}\n  if\(loading\)/,`  async function activate(){if(!vehicleId||!tag.trim()||!pin.trim()){Alert.alert('Eksik bilgi','Etiket ID, aktivasyon PIN ve araç seçimi gerekli.');return;}setBusy(true);try{const result=await activateTag(tag.startsWith('DP-')?tag:'DP-'+tag,pin,vehicleId);Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success);setReward(result);}catch(e:any){Alert.alert('Aktivasyon başarısız',String(e?.message||'Tag ID veya PIN eşleşmedi.'));}finally{setBusy(false)}}
  if(loading)`);
replaceOnce('app/activate/[id].tsx','  </ScrollView></SafeAreaView>;','  </ScrollView><ColorPopup visible={Boolean(reward)} icon="crown-circle-outline" eyebrow="14 GÜN DRABORNPARK+ HEDİYE" title="Premium ödülün hazır!" body="Yeni DraBornPark etiketini başarıyla aktive ettin. Hesabına 14 günlük DraBornPark+ ödülü eklendi. Süre sonunda Google Play üzerinden aylık veya yıllık uygun planla devam edebilirsin." accent={palette.yellow} secondary={palette.purple} primaryLabel="DRABORNPARK+ SAYFASINI AÇ" onPrimary={()=>{setReward(null);router.replace(\'/feature/plus\')}} secondaryLabel="ANA SAYFA" onSecondary={()=>{setReward(null);router.replace(\'/\')}} chips={[\'14 GÜN PREMIUM\',\'AYLIK PLAN\',\'YILLIK PLAN\']}/></SafeAreaView>;');

replaceOnce('app/park.tsx',"import { AuroraBackground, BottomDock } from '@/src/components/AppChrome';","import { AuroraBackground, BottomDock } from '@/src/components/AppChrome';\nimport { ColorPopup } from '@/src/components/ColorPopup';");
replaceOnce('app/park.tsx',"const [coords,setCoords]=useState<{lat:number;lon:number;accuracy:number|null}|null>(null);","const [coords,setCoords]=useState<{lat:number;lon:number;accuracy:number|null}|null>(null);\n  const [locationInfoOpen,setLocationInfoOpen]=useState(false);");
replaceRegex('app/park.tsx',/  function locate\(\)\{[\s\S]*?\n  \}\n  async function locateNow/,`  function locate(){if(demo.active){setCoords({lat:39.9357,lon:32.8063,accuracy:8});Haptics.selectionAsync();return;}setLocationInfoOpen(true);}
  async function locateNow`);
replaceOnce('app/park.tsx','    <BottomDock active="park"/>\n  </ScrollView></Animated.View></SafeAreaView>;','    <BottomDock active="park"/>\n  </ScrollView><ColorPopup visible={locationInfoOpen} icon="map-marker-radius-outline" eyebrow="KONUM İZNİ" title="Park yerini hatırlayalım" body="DraBornPark konumunu yalnızca bu park kaydındaki noktayı kaydetmek ve Aracıma Git yönlendirmesini oluşturmak için kullanır. Arka planda sürekli takip yapmaz; konum QR/NFC ziyaretçilerine gösterilmez." accent={palette.green} secondary={palette.cyan} primaryLabel="KONUMA İZİN VER" onPrimary={()=>{setLocationInfoOpen(false);void locateNow()}} secondaryLabel="ELLE GİRECEĞİM" onSecondary={()=>setLocationInfoOpen(false)} chips={[\'TEK SEFERLİK\',\'ARKA PLAN TAKİBİ YOK\',\'ÖZEL\']}/></Animated.View></SafeAreaView>;');

const pkg=JSON.parse(read('package.json'));pkg.version='0.5.4';pkg.dependencies['expo-iap']='5.3.2';pkg.dependencies['expo-build-properties']='~57.0.8';write('package.json',JSON.stringify(pkg,null,2)+'\n');
const app=JSON.parse(read('app.json'));app.expo.version='0.5.4';app.expo.android.versionCode=20;app.expo.ios={...(app.expo.ios||{}),deploymentTarget:'15.1'};const plugins=app.expo.plugins??[];if(!plugins.some(p=>(Array.isArray(p)?p[0]:p)==='expo-iap'))plugins.push(['expo-iap',{}]);if(!plugins.some(p=>(Array.isArray(p)?p[0]:p)==='expo-build-properties'))plugins.push(['expo-build-properties',{android:{kotlinVersion:'2.2.0'}}]);app.expo.plugins=plugins;write('app.json',JSON.stringify(app,null,2)+'\n');write('.github/VERSION','0.5.4\n');

let config=read('supabase/config.toml');if(!config.includes('[functions.dkd-drabornpark-admin-support-push]'))config+='\n[functions.dkd-drabornpark-admin-support-push]\nverify_jwt = true\n\n[functions.dkd-drabornpark-google-play]\nverify_jwt = true\n';write('supabase/config.toml',config);
let ci=read('.github/workflows/ci.yml').replaceAll('DraBornPark-v0.5.3','DraBornPark-v0.5.4').replaceAll('vc19-developer-debug','vc20-developer-debug').replaceAll('vc19-developer-apk','vc20-developer-apk').replaceAll('DraBornPark v0.5.3','DraBornPark v0.5.4').replaceAll('Android versionCode: 19','Android versionCode: 20');write('.github/workflows/ci.yml',ci);
let readme=read('README.md').replace('## Aktif sürüm — v0.5.3','## Aktif sürüm — v0.5.4').replace('Android `versionCode`: `19`','Android `versionCode`: `20`');if(!readme.includes('## v0.5.4 — destek, izinler ve DraBornPark+'))readme+='\n## v0.5.4 — destek, izinler ve DraBornPark+\n\n- Kayıtta zorunlu telefon numarası, kullanıcı adı ve opsiyonel profil resmi.\n- Araç ekleme, konum/bildirim izinleri ve etiket Premium ödülü için renkli animasyonlu uygulama popup’ları.\n- Alt menü: Ana Sayfa / Park Alanı / Bildirimler / Merkezim.\n- Destek Merkezi aynı uygulama içinde; admin yeni kayıt bildirimi ve detay ekranı.\n- Yeni etiket aktivasyonu 14 günlük DraBornPark+ ödülü verir.\n- Google Play ürünü `drabornpark_plus`; base plan kimlikleri `monthly` ve `yearly`. Fiyatlar Play Store’dan yerel para birimiyle okunur.\n- Satın alma tokenı Edge Function tarafından Google Play Developer API ile doğrulanmadan Premium hakkı açılmaz ve token veritabanında yalnızca SHA-256 hash olarak saklanır.\n- Gerçek Play testi Developer/Internal Test build ve Play Console servis hesabı gerektirir. Supabase secret adı: `GOOGLE_PLAY_SERVICE_ACCOUNT_JSON`.\n';write('README.md',readme);

const check=`import fs from 'node:fs';import path from 'node:path';const root=process.cwd();const read=p=>fs.readFileSync(path.join(root,p),'utf8');const exists=p=>fs.existsSync(path.join(root,p));const walk=d=>!fs.existsSync(d)?[]:fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>{const f=path.join(d,e.name);return e.isDirectory()?walk(f):[f]});const fail=[];const pkg=JSON.parse(read('package.json'));const app=JSON.parse(read('app.json'));const repoVersion=read('.github/VERSION').trim();if(pkg.version!=='0.5.4'||app.expo?.version!=='0.5.4'||repoVersion!=='0.5.4')fail.push('v0.5.4 version coherence failed');if(app.expo?.android?.versionCode!==20)fail.push('Android versionCode must be 20');if(pkg.dependencies?.['expo-iap']!=='5.3.2')fail.push('expo-iap 5.3.2 required');if(pkg.dependencies?.['expo-build-properties']!=='~57.0.8')fail.push('expo-build-properties ~57.0.8 required');if(app.expo?.experiments?.reactCompiler===true)fail.push('React Compiler must stay disabled');const required=['app/legal.tsx','app/account.tsx','app/tags.tsx','app/factory.tsx','app/hub.tsx','app/notifications.tsx','app/support/[id].tsx','src/components/ColorPopup.tsx','src/components/SupportCenterPanel.tsx','src/components/DraBornParkPlusPanel.tsx','supabase/functions/dkd-drabornpark-admin-support-push/index.ts','supabase/functions/dkd-drabornpark-google-play/index.ts','supabase/migrations/20260822113456_dkd_drabornpark_v054_support_plus_billing.sql'];for(const f of required)if(!exists(f))fail.push('Required file missing: '+f);for(const retired of ['.github/v054-source','.github/v054-payload','scripts/release-v054.mjs','scripts/apply-v054.mjs','.github/workflows/release-v054.yml'])if(exists(retired))fail.push('Temporary release artifact remains: '+retired);const routes=walk(path.join(root,'app')).filter(f=>f.endsWith('.tsx'));for(const f of routes)if(!/export\\s+default\\s+/.test(fs.readFileSync(f,'utf8')))fail.push('Route has no default export: '+path.relative(root,f));const chrome=read('src/components/AppChrome.tsx');for(const m of ["label:'Ana Sayfa'","label:'Park Alanı'","label:'Bildirimler'"])if(!chrome.includes(m))fail.push('Bottom navigation marker missing: '+m);const auth=read('app/auth.tsx');for(const m of ['TELEFON NUMARASI *','phone_e164','normalizedPhone'])if(!auth.includes(m))fail.push('Signup phone marker missing: '+m);const home=read('app/index.tsx');for(const m of ['DraBornPark+','AKTİF ARAÇ • CANLI','Konum ayrıntısı kaydedilmedi'])if(!home.includes(m))fail.push('Home v0.5.4 marker missing: '+m);const park=read('app/park.tsx');if(!park.includes('locationInfoOpen')||!park.includes('ARKA PLAN TAKİBİ YOK'))fail.push('Modern location disclosure missing');const push=read('src/lib/push.ts');for(const m of ['requestNotificationPermission','startAdminSupportNotifications','startNotificationResponseRouting','drabornpark_support'])if(!push.includes(m))fail.push('Push v0.5.4 marker missing: '+m);const lib=read('src/lib/drabornpark.ts');for(const m of ['dkd_drabornpark_bootstrap_user_v054','dkd_drabornpark_activate_tag_v054','dkd-drabornpark-admin-support-push'])if(!lib.includes(m))fail.push('Backend helper marker missing: '+m);const plus=read('src/components/DraBornParkPlusPanel.tsx');for(const m of ['drabornpark_plus','monthly','yearly','dkd-drabornpark-google-play','finishTransaction'])if(!plus.includes(m))fail.push('Google Play marker missing: '+m);if(!exists('src/components/EvidencePhoto.tsx')||!exists('src/lib/callRequests.ts')||!exists('src/lib/contactThreads.ts'))fail.push('v0.5.3 secure communication layer regressed');if(fail.length){console.error('DraBornPark integrity check failed:\\n- '+fail.join('\\n- '));process.exit(1)}console.log('DraBornPark integrity OK • v0.5.4 • Android vc20 • support/admin push • 14-day tag reward • Google Play billing prepared.');`;
write('scripts/check-project.mjs',check+'\n');
removePath('.github/v054-source');removePath('.github/v054-payload');removePath('scripts/release-v054.mjs');removePath('scripts/apply-v054.mjs');removePath('.github/workflows/release-v054.yml');
console.log('DraBornPark v0.5.4 guarded release patch applied.');
