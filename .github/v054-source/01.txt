replace("app/auth.tsx","        <Field label=\"KULLANICI ADI *\" value={username} set={setUsername} placeholder=\"draborneagle\" icon=\"account-outline\"/>","        <Field label=\"KULLANICI ADI *\" value={username} set={setUsername} placeholder=\"draborneagle\" icon=\"account-outline\"/>\n        <Field label=\"TELEFON NUMARASI *\" value={phone} set={setPhone} placeholder=\"0555 111 22 33\" icon=\"phone-outline\" keyboard=\"phone-pad\"/>",1);
replace("app/auth.tsx","<Text style={s.trialTitle}>14 Gün DraBornPark+ Hediye</Text><Text style={s.trialBody}>Yeni kullanıcı deneme süresinde Aile, Vale/Servis, Zaman Kuralları ve gelişmiş araç özelliklerini deneyebilir. Basic etiket işlevleri deneme bitince kapanmaz.</Text>","<Text style={s.trialTitle}>Yeni Etiketine 14 Gün DraBornPark+</Text><Text style={s.trialBody}>Hesabın Basic olarak açılır. Yeni DraBornPark etiketini ilk kez aktive ettiğinde 14 günlük Premium ödül başlar; süre sonunda aylık veya yıllık Google Play planıyla devam edebilirsin.</Text>",1);
replace("src/lib/drabornpark.ts","export function hasPlusEntitlement(profile: any | null, subscription: any | null) {\n  if (!profile) return false;\n  if (['PLUS', 'PLUS_ACTIVE', 'ACTIVE'].includes(profile.subscription_status)) return true;\n  if (profile.subscription_status === 'PLUS_TRIAL' && profile.plus_trial_until) {\n    if (new Date(profile.plus_trial_until).getTime() > Date.now()) return true;\n  }\n  if (subscription && ['active', 'trialing', 'grace_period'].includes(String(subscription.status).toLowerCase())) {\n    return !subscription.expires_at || new Date(subscription.expires_at).getTime() > Date.now();\n  }\n  return false;\n}","export function hasPlusEntitlement(profile: any | null, subscription: any | null) {\n  if (!profile) return false;\n  const profileStatus=String(profile.subscription_status||'').toUpperCase();\n  if (profileStatus==='PLUS_TRIAL' && profile.plus_trial_until && new Date(profile.plus_trial_until).getTime()>Date.now()) return true;\n  const subscriptionStatus=String(subscription?.status||'').toUpperCase();\n  if (['PLUS_ACTIVE','PLUS_GRACE_PERIOD','PLUS_TRIAL','PLUS_CANCELLED'].includes(subscriptionStatus)) {\n    return !subscription?.expires_at || new Date(subscription.expires_at).getTime()>Date.now();\n  }\n  return profileStatus==='PLUS_ACTIVE'||profileStatus==='PLUS_GRACE_PERIOD';\n}",1);
replace("src/lib/drabornpark.ts","export async function bootstrapProfile(displayName?: string, username?: string, avatarUrl?: string) {\n  const { data: userData } = await supabase.auth.getUser();\n  const user = userData.user;\n  const meta = user?.user_metadata ?? {};\n  const resolvedUsername = (\n    username ??\n    meta.username ??\n    displayName ??\n    meta.display_name ??\n    user?.email?.split('@')[0] ??\n    ''\n  ).trim().toLowerCase() || null;\n  const resolvedDisplay = displayName ?? meta.display_name ?? resolvedUsername ?? undefined;\n  const { data, error } = await supabase.rpc('drabornpark_bootstrap_user', {\n    drabornpark_display_name: resolvedDisplay ?? null,\n    drabornpark_username: resolvedUsername,\n    drabornpark_avatar_url: avatarUrl ?? meta.avatar_url ?? null,\n  });\n  if (error) throw error;\n  return data;\n}","export async function bootstrapProfile(displayName?: string, username?: string, avatarUrl?: string, phoneE164?: string) {\n  const { data: userData } = await supabase.auth.getUser();\n  const user = userData.user;\n  const meta = user?.user_metadata ?? {};\n  const resolvedUsername = (\n    username ??\n    meta.username ??\n    displayName ??\n    meta.display_name ??\n    user?.email?.split('@')[0] ??\n    ''\n  ).trim().toLowerCase() || null;\n  const resolvedDisplay = displayName ?? meta.display_name ?? resolvedUsername ?? undefined;\n  const { data, error } = await supabase.rpc('dkd_drabornpark_bootstrap_user_v054', {\n    dkd_display_name: resolvedDisplay ?? null,\n    dkd_username: resolvedUsername,\n    dkd_avatar_url: avatarUrl ?? meta.avatar_url ?? null,\n    dkd_phone_e164: phoneE164 ?? meta.phone_e164 ?? null,\n  });\n  if (error) throw error;\n  return data;\n}",1);
replace("src/lib/drabornpark.ts","supabase.rpc('drabornpark_activate_tag', {","supabase.rpc('dkd_drabornpark_activate_tag_v054', {",1);
replace("src/lib/drabornpark.ts","export async function createSupportRequest(subject: string, body: string) {\n  const ownerUserId = await currentUserId();\n  const { data, error } = await supabase.from('drabornpark_support_requests').insert({\n    owner_user_id: ownerUserId,\n    subject: subject.trim(),\n    body: body.trim(),\n    status: 'open',\n  }).select('*').single();\n  if (error) throw error;\n  return data;\n}","export async function createSupportRequest(subject: string, body: string) {\n  const ownerUserId = await currentUserId();\n  const { data, error } = await supabase.from('drabornpark_support_requests').insert({\n    owner_user_id: ownerUserId,\n    subject: subject.trim(),\n    body: body.trim(),\n    status: 'open',\n  }).select('*').single();\n  if (error) throw error;\n  void supabase.functions.invoke('dkd-drabornpark-admin-support-push',{body:{supportId:data.id}}).catch(()=>{});\n  return data;\n}\n\nexport async function loadSupportRequests(limit=20) {\n  const ownerUserId=await currentUserId();\n  const {data,error}=await supabase.from('drabornpark_support_requests').select('*').eq('owner_user_id',ownerUserId).order('created_at',{ascending:false}).limit(limit);\n  if(error)throw error;\n  return data??[];\n}\n\nexport async function loadSupportRequestDetail(id:string) {\n  const {data,error}=await supabase.from('drabornpark_support_requests').select('*').eq('id',id).maybeSingle();\n  if(error)throw error;\n  return data;\n}\n\nexport async function markSupportRequestAdminSeen(id:string) {\n  const {data:isAdmin,error:adminError}=await supabase.rpc('drabornpark_is_admin');\n  if(adminError)throw adminError;\n  if(!isAdmin)return false;\n  const {error}=await supabase.from('drabornpark_support_requests').update({admin_seen_at:new Date().toISOString(),updated_at:new Date().toISOString()}).eq('id',id);\n  if(error)throw error;\n  return true;\n}\n\nexport async function updateSupportRequestStatus(id:string,status:'open'|'in_progress'|'resolved'|'closed') {\n  const {data,error}=await supabase.from('drabornpark_support_requests').update({status,admin_seen_at:new Date().toISOString(),updated_at:new Date().toISOString()}).eq('id',id).select('*').single();\n  if(error)throw error;\n  return data;\n}",1);
replace("app/vehicle.tsx","import { AuroraBackground, ScreenHeader, SectionHeading } from '@/src/components/AppChrome';","import { AuroraBackground, ScreenHeader, SectionHeading } from '@/src/components/AppChrome';\nimport { ColorPopup } from '@/src/components/ColorPopup';",1);
replace("app/vehicle.tsx","const [vehicleType,setVehicleType]=useState<'car'|'motorcycle'>('car');const [name,setName]=useState('');const [plate,setPlate]=useState('');const [brand,setBrand]=useState('');const [model,setModel]=useState('');const [year,setYear]=useState('');const [color,setColor]=useState('');const [busy,setBusy]=useState(false);const [vehicles,setVehicles]=useState<any[]>([]);","const [vehicleType,setVehicleType]=useState<'car'|'motorcycle'>('car');const [name,setName]=useState('');const [plate,setPlate]=useState('');const [brand,setBrand]=useState('');const [model,setModel]=useState('');const [year,setYear]=useState('');const [color,setColor]=useState('');const [busy,setBusy]=useState(false);const [vehicles,setVehicles]=useState<any[]>([]);const [createdVehicle,setCreatedVehicle]=useState<any>(null);",1);
replace("app/vehicle.tsx","async function save(){if(!name.trim()){Alert.alert('Araç adı gerekli','Örn. Tiguan, Aile Aracı veya Motorum.');return;}setBusy(true);try{const created=await createVehicle({vehicleName:name,plate,brand,model,modelYear:year?Number(year):null,color,vehicleType});setVehicles(current=>[...current,created]);setName('');setPlate('');setBrand('');setModel('');setYear('');setColor('');Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success);Alert.alert('Araç eklendi','Şimdi DraBornPark etiketini bu araca bağlayabilirsin.',[{text:'Etiket Aktivasyonu',onPress:()=>router.push('/activate/new')},{text:'Tamam'}]);}catch(e:any){Alert.alert('Araç eklenemedi',e?.message||'İşlem başarısız.');}finally{setBusy(false)}}","async function save(){if(!name.trim()){Alert.alert('Araç adı gerekli','Örn. Tiguan, Aile Aracı veya Motorum.');return;}setBusy(true);try{const created=await createVehicle({vehicleName:name,plate,brand,model,modelYear:year?Number(year):null,color,vehicleType});setVehicles(current=>[...current,created]);setName('');setPlate('');setBrand('');setModel('');setYear('');setColor('');Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success);setCreatedVehicle(created);}catch(e:any){Alert.alert('Araç eklenemedi',e?.message||'İşlem başarısız.');}finally{setBusy(false)}}",1);
replace("app/vehicle.tsx","  </ScrollView></SafeAreaView>;","  </ScrollView><ColorPopup visible={Boolean(createdVehicle)} icon=\"car-check\" eyebrow=\"ARAÇ AĞINA EKLENDİ\" title=\"Aracın hazır!\" body={`${createdVehicle?.vehicle_name||'Aracın'} güvenli DraBornPark ağına eklendi. Şimdi NFC + QR etiketini bağlayabilir veya daha sonra devam edebilirsin.`} accent={palette.cyan} secondary={palette.pink} primaryLabel=\"ETİKETİ AKTİVE ET\" onPrimary={()=>{setCreatedVehicle(null);router.push('/activate/new')}} secondaryLabel=\"ARAÇLARIMDA KAL\" onSecondary={()=>setCreatedVehicle(null)}/></SafeAreaView>;",1);
replace("app/activate/[id].tsx","import { AuroraBackground, ScreenHeader, SectionHeading } from '@/src/components/AppChrome';","import { AuroraBackground, ScreenHeader, SectionHeading } from '@/src/components/AppChrome';\nimport { ColorPopup } from '@/src/components/ColorPopup';",1);
replace("app/activate/[id].tsx","const [vehicleId,setVehicleId]=useState('');const [busy,setBusy]=useState(false);const [loading,setLoading]=useState(true);","const [vehicleId,setVehicleId]=useState('');const [busy,setBusy]=useState(false);const [loading,setLoading]=useState(true);const [reward,setReward]=useState<any>(null);",1);
replace("app/activate/[id].tsx","async function activate(){if(!vehicleId||!tag.trim()||!pin.trim()){Alert.alert('Eksik bilgi','Etiket ID, aktivasyon PIN ve araç seçimi gerekli.');return;}setBusy(true);try{await activateTag(tag.startsWith('DP-')?tag:`DP-${tag}`,pin,vehicleId);Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success);Alert.alert('DraBornPark aktif','Etiket hesabına ve seçtiğin araca güvenli şekilde bağlandı. 14 günlük Plus denemen hazır.',[{text:'Ana Sayfa',onPress:()=>router.replace('/')}]);}catch(e:any){Alert.alert('Aktivasyon başarısız',String(e?.message||'Tag ID veya PIN eşleşmedi.'));}finally{setBusy(false)}}","async function activate(){if(!vehicleId||!tag.trim()||!pin.trim()){Alert.alert('Eksik bilgi','Etiket ID, aktivasyon PIN ve araç seçimi gerekli.');return;}setBusy(true);try{const result=await activateTag(tag.startsWith('DP-')?tag:`DP-${tag}`,pin,vehicleId);Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success);setReward(result);}catch(e:any){Alert.alert('Aktivasyon başarısız',String(e?.message||'Tag ID veya PIN eşleşmedi.'));}finally{setBusy(false)}}",1);
