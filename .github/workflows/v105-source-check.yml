name: DraBornPark v1.0.6 Source Check

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'package.json'
      - 'package-lock.json'
      - 'app.json'
      - '.github/VERSION'
      - '.github/workflows/v105-source-check.yml'
      - 'app/**'
      - 'src/**'
      - 'scripts/**'
      - 'assets/branding/v101/**'
      - 'supabase/migrations/20260825024500_dkd_drabornpark_v106_realtime_calls.sql'
  pull_request:
    branches: [main]

permissions:
  contents: write

concurrency:
  group: drabornpark-v106-source-check-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Sync dependency lock
        run: npm install --package-lock-only --ignore-scripts --no-audit --no-fund
      - name: Install dependencies and materialize v1.0.6 assets
        run: npm install --no-audit --no-fund
      - name: Validate DraBornPark v1.0.6 source
        run: |
          npm run check
          npx expo install --check
          npm run typecheck
          git diff --check
      - name: Persist package lock on main
        if: github.event_name == 'push'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json
          if ! git diff --cached --quiet; then
            git commit -m "chore: sync DraBornPark v1.0.6 package lock"
            git push origin HEAD:main
          fi
      - name: Confirm source validation produced no Android release
        run: |
          test ! -d android/app/build/outputs || { echo "Unexpected Android build output" >&2; exit 1; }
          echo "DraBornPark v1.0.6 source validation complete."
